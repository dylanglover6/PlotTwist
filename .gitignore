# Dependencies & build output
node_modules
dist
coverage

# Secrets / environment — never commit real env files
.env
.env.local
# Production env for the VM (keep only the .example in git)
deploy/*.env

# AI agent / tooling config (local only)
.claude/
.agents/
.cursor/
.cursorrules
skills-lock.json
.aider*

# Internal / planning docs — not for the public repo
unified-deployment-plan.md
*-brief.md
plans/
docs/plan/

# Logs & session output
*.log
npm-debug.log*
logs/

# OS / editor cruft
.DS_Store
Thumbs.db
.idea/

# Personal working files (resumes, exports, etc.)
*.pdf
